// Enum for Generic Items
enum GenericItem {
  trip,
  playback,
  stoppage,
  idle,
  geofence,
  ac,
  dailySummary,
  overSpeed,
  distance,
  alert,
  fence,
  park_sum,
  idle_sum,
  trip_sum,
  distance_sum,
  speed_sum,
  alert_sum,
  ac_sum,
  ac_graph,
  ac_duration,
  speed_graph,
  inactive_report,
}

// Enum for Executive Items
enum ExecutiveItem {
  executive,
}

// Enum for Add-On Items
enum AddOnItem {
  eLock,
  aiAlert,
  videoPlayback,
}

// Enum for Sensor Items
enum SensorItem { fuel, fuelSummary, mileage, temperature, rpm }

enum SensorGraph { fuel, temperature }
